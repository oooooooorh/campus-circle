# 🚀 前端课程表功能 - 使用指南

## ✅ 已完成

前端 `Schedule.vue` 已完整实现以下功能：

### 📝 功能清单

| 功能 | 实现状态 | 说明 |
|------|--------|------|
| 账号密码输入框 | ✅ | 用户可输入学号和教务系统密码 |
| 获取课表按钮 | ✅ | 点击执行爬虫任务 |
| 加载状态显示 | ✅ | 爬虫进行中显示加载动画 |
| 课程表格显示 | ✅ | 完整的课程信息表格 |
| 周次日程视图 | ✅ | 可视化的周日程表 |
| 课程统计信息 | ✅ | 显示总课程数、周次跨度等 |
| 错误提示 | ✅ | 登录失败/网络错误提示 |
| 成功提示 | ✅ | 加载成功提示 |
| JSON 下载 | ✅ | 导出课表为 JSON 文件 |
| 响应式设计 | ✅ | 适配手机、平板、电脑 |

---

## 🎨 UI 设计亮点

### 配色方案
- 紫色渐变背景（现代风格）
- 白色卡片（层级清晰）
- 彩色课程块（易于区分）

### 交互体验
- 输入框回车键快捷提交
- 按钮禁用状态管理
- 平滑过渡动画
- 3秒后自动关闭提示框

### 响应式布局
- 电脑版：多列网格
- 平板版：自适应列数
- 手机版：单列展示

---

## 🎯 使用步骤

### 1️⃣ 启动后端服务

```bash
cd backend
conda activate campus_env
python -m uvicorn main:app --reload
```

### 2️⃣ 启动前端服务

```bash
cd frontend
npm run dev
```

✅ **前端地址：** http://localhost:5173

### 3️⃣ 使用课程表功能

1. 导航到 "我的课表" 页面（Schedule）
2. 输入学号（例：2320110098）
3. 输入教务系统密码
4. 点击 "🔄 获取课表" 按钮
5. 等待爬虫完成（约 5-10 秒）
6. 查看课程表格和周日程
7. 可选：点击 "💾 下载 JSON" 保存数据

---

## 📊 页面布局说明

### 第一部分：登录表单
```
┌─────────────────────────────────┐
│  🔐 教务系统登录                │
│                                  │
│  学号：   [输入框]              │
│  密码：   [输入框]              │
│                                  │
│  [获取课表按钮] [下载JSON按钮]  │
│                                  │
│  [错误/成功提示]                │
└─────────────────────────────────┘
```

### 第二部分：统计信息
```
┌──────────────────────┬──────────────────────┐
│ 总课程数：11         │ 周次跨度：1-18周     │
└──────────────────────┴──────────────────────┘
```

### 第三部分：课程表格
```
┌────┬──────────────┬────┬────┬──────┬──┬──┬──┬───┐
│代码│  课程名称    │学分│班级│周次  │星│节│师│地 │
├────┼──────────────┼────┼────┼──────┼──┼──┼──┼───┤
│G006│高等数学（一）│ 4  │ 1班│1-18周│一│1-│李│307│
├────┼──────────────┼────┼────┼──────┼──┼──┼──┼───┤
│...
└────┴──────────────┴────┴────┴──────┴──┴──┴──┴───┘
```

### 第四部分：周日程视图
```
┌────────┬────────┬────────┬────────┬────────┬────────┬────────┐
│ 星期一 │ 星期二 │ 星期三 │ 星期四 │ 星期五 │ 星期六 │ 星期日 │
├────────┼────────┼────────┼────────┼────────┼────────┼────────┤
│[课程1] │[课程2] │[课程3] │[课程4] │[课程5] │        │        │
│[课程6] │        │[课程7] │        │[课程8] │        │        │
└────────┴────────┴────────┴────────┴────────┴────────┴────────┘
```

---

## 🔄 数据流程

```
用户输入账号密码
    ↓
点击"获取课表"按钮
    ↓
调用 getSchedule(username, password)
    ↓
发送 POST /api/schedule 请求到后端
    ↓
后端启动 Playwright 浏览器
    ↓
登录教务系统 + 爬虫课表
    ↓
后端返回 JSON 数据
    ↓
前端使用 Vue 渲染表格
    ↓
显示课程表和周日程
```

---

## 🎨 表格字段说明

| 字段 | 含义 | 例子 |
|------|------|------|
| kh | 课程代码 | G006 |
| kcmc | 课程名称 | 高等数学（一） |
| xf | 学分 | 4 |
| jxb | 班级 | 1班 |
| zc | 周次 | 1-18周 |
| xq | 星期 | 星期一 |
| jc | 节次 | 1-2节 |
| kcjs | 教师（可选） | 李老师 |
| skdd | 地点（可选） | 307教室 |

---

## 🎨 样式亮点

### 颜色主题
```css
主色：#667eea（紫蓝）
副色：#764ba2（深紫）
强调色：#FF6B6B（红色）
背景：渐变紫色
```

### 交互反馈
```
按钮悬停：升起效果 + 阴影加深
表格行悬停：背景变色
课程块悬停：位置上升
输入框焦点：蓝色边框 + 发光阴影
```

---

## 🔐 账号密码处理

### 安全说明
- ✅ 密码输入框隐藏显示
- ✅ 仅在获取课表时发送到后端
- ✅ 后端不存储密码
- ✅ 爬虫完成后立即释放资源

### 建议
- 使用教务系统原始密码（无加密）
- 不建议保存密码到浏览器
- 生产环境建议添加 HTTPS + 加密

---

## 📱 响应式断点

| 设备 | 宽度 | 调整 |
|------|------|------|
| 手机 | < 768px | 单列、更大触摸按钮 |
| 平板 | 768px - 1024px | 双列、自适应 |
| 电脑 | > 1024px | 三列+、最优显示 |

---

## 🐛 常见问题

### Q: 输入框输入了但按钮还是灰色？
**A:** 确保学号和密码都输入了，两个都必填

### Q: 显示"获取课表失败"？
**A:** 检查：
1. 后端是否运行（http://127.0.0.1:8000/docs）
2. 账号密码是否正确
3. 教务系统是否正常

### Q: 表格没有显示？
**A:** 可能是：
1. 后端爬虫失败（查看后端日志）
2. 网络延迟（多等几秒）
3. 课表数据为空

### Q: 如何下载课表数据？
**A:** 点击"💾 下载 JSON"按钮，自动下载为 JSON 文件

### Q: 手机上显示不全？
**A:** 页面已做响应式适配，检查浏览器视口是否正确

---

## 🚀 性能优化

### 前端优化
- ✅ 使用 Vue 3 Composition API（高效）
- ✅ 虚拟滚动（大数据表格）
- ✅ 样式作用域隔离（scoped）
- ✅ 动态 class 绑定（只渲染需要的样式）

### 网络优化
- ✅ 一次请求获取全部数据
- ✅ JSON 格式传输（高效）
- ✅ 支持浏览器缓存

---

## 📚 相关文档

| 文件 | 位置 | 说明 |
|------|------|------|
| 前端代码 | `frontend/src/views/Schedule.vue` | 完整实现 |
| API 调用 | `frontend/src/api/schedule-api.js` | 后端通信 |
| 后端 API | `backend/main.py` | `/api/schedule` 接口 |
| 爬虫函数 | `backend/scraper.py` | 核心爬虫逻辑 |

---

## ✨ 可扩展方向

### 1. 添加日历视图
```javascript
// 使用 @vueuse/components 的 Calendar
<Calendar :schedule="schedule" />
```

### 2. 添加课程提醒
```javascript
// 使用 Notification API 提醒上课
if (isCourseTime()) {
  notify('还有 10 分钟上课')
}
```

### 3. 添加课程分享
```javascript
// 生成课程分享链接
const link = encodeURIComponent(JSON.stringify(schedule.value))
```

### 4. 添加课程评价
```javascript
// 集成课程评价功能
<CourseRating :course="course" />
```

### 5. 添加课表冲突检测
```javascript
// 检测课程时间冲突
function detectConflicts(schedules) {
  // ...
}
```

---

## 🎯 测试清单

启动前确认：
- [ ] 后端服务运行中（http://127.0.0.1:8000）
- [ ] 前端服务运行中（http://localhost:5173）
- [ ] 能访问 Schedule 页面
- [ ] 输入框能输入
- [ ] 按钮能点击
- [ ] 网络请求正常（查看浏览器开发者工具）

功能测试：
- [ ] 输入错误账号会显示错误提示
- [ ] 输入正确账号能获取课表
- [ ] 表格能正确显示课程
- [ ] 周日程能正确分配课程
- [ ] 下载 JSON 功能正常
- [ ] 页面在手机上能正常显示

---

**完成！🎉 现在你有一个完整的课程表管理系统了！**

更多问题请查看 `INTEGRATION_COMPLETE.md` 文件。
